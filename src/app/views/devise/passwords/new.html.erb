<%# パスワード再設定（メール送信）画面 %>
<div class="flex-grow-1">
  <section class="p-5 mx-auto mt-5 bg-color bg-width">
    <h1 class="title mb-4">パスワード再設定</h1>

    <%# 再設定用メール送信フォーム %>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field mb-3">
        <%= f.label :email, class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-100 form-control", placeholder: "name@example.com" %>
      </div>

      <div class="actions mb-2">
        <%= f.submit "再設定用メールを送信する", class: "w-100 btn btn-primary" %>
      </div>
    <% end %>

    <%# deviseリンク %>
    <div class="form-text">
      <%= render "devise/shared/links" %>
    </div>
  </section>
</div>
